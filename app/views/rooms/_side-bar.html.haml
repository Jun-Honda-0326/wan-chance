.side-bar
  .side-bar__box
    .current_names
      DM相手一覧
    
    .dm-list
      - @rooms.each do |room|
        .dm-list__name
          = link_to room_path(room) do
            %h3.dm-list__name__link
              = opponent_user(room)