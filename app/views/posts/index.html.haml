.contents
  - @posts.each do |post|
    .content
      .img-box  
      .text-box<>
        .text
          = post.text
        .user-info
          .user-name
            投稿者：
          .post-time
            投稿時間：
            = post.created_at
    
