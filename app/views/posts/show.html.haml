.contents
  = render @post
  -# - if current_user
  -#   = render partial: "likes/like" 
  

  .comment-box
    - if current_user
      = form_with(model: [@post, @comment], local: true, id: "new_comment") do |f|
        = f.text_area :text, placeholder: 'コメントする' , class: "comment__text-box"
        = f.submit 'コメント', class: 'btn', class: "comment__form"
    - else
      <strong><p>※※※ コメントの投稿には新規登録/ログインが必要です ※※※</p></strong>
      
    .comments  
      %h5<>
        コメント一覧
      - if @comments
        - @comments.each do |comment|
          .comment
            .comment__name
              = link_to comment.user.name+":",  "/users/#{comment.user.id}"
            .comment__text
              = comment.text
    